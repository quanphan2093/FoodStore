@page
@model FoodStoreClient.Pages.Admin.Restoran_Management_Account.Management_Account_CreateModel
@{
    ViewBag.PageTitle = "Admin";
    ViewBag.Breadcrumbs = new List<Tuple<string, string>>
    {
        Tuple.Create("/Admin/Restoran_Management_Account/Management_Account", "Admin"),
        Tuple.Create("Management_Account_Create", "Add New Account")
    };
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Restoran - Bootstrap Restaurant Template</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="" name="keywords">
    <meta content="" name="description">

    <!-- Favicon -->
    <link href="~/img/favicon.ico" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600&family=Nunito:wght@600;700;800&family=Pacifico&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="~/lib/animate/animate.min.css" rel="stylesheet">
    <link href="~/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">
    <link href="~/lib/tempusdominus/css/tempusdominus-bootstrap-4.min.css" rel="stylesheet" />

    <!-- Customized Bootstrap Stylesheet -->
    <link href="~/css/bootstrap.min.css" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="~/css/style.css" rel="stylesheet">
</head>

<body>
    <div class="container-xxl bg-white p-0">


        <!-- Back to Top -->
        <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>
    </div>

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/wow/wow.min.js"></script>
    <script src="~/lib/easing/easing.min.js"></script>
    <script src="~/lib/waypoints/waypoints.min.js"></script>
    <script src="~/lib/counterup/counterup.min.js"></script>
    <script src="~/lib/owlcarousel/owl.carousel.min.js"></script>
    <script src="~/lib/tempusdominus/js/moment.min.js"></script>
    <script src="~/lib/tempusdominus/js/moment-timezone.min.js"></script>
    <script src="~/lib/tempusdominus/js/tempusdominus-bootstrap-4.min.js"></script>
    <style>

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

        .profile-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 20px;
            padding: 20px;
        }

            .profile-header img {
                width: 150px;
                height: 150px;
                border-radius: 50%;
                object-fit: cover;
                margin-left: 100px;
            }

        .profile-info {
            flex: 1;
            max-width: 500px;
            padding: 20px;
            text-align: left;
            margin-left: 150px;
        }

            .profile-info p {
                font-size: 16px;
                margin: 8px 0;
            }

                .profile-info p span {
                    font-weight: bold;
                    color: #555;
                }
    </style>
    <div class="container" style="max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            margin-top: 50px;">
        <h1>Profile User</h1>

        <div class="profile-header">
            <!-- Ảnh đại diện bên phải -->
            <img src="https://i.pinimg.com/1200x/16/b2/e2/16b2e2579118bf6fba3b56523583117f.jpg" alt="Profile Picture">
        </div>
        <!-- Hidden Update Form -->
        <div id="updateProfileForm" style="display:block;">
            <div class="container" style="max-width: 900px; opacity: 0.9;">
                <div class="row justify-content-center">
                    <div class="col-lg-5">
                        <form method="post" asp-page-handler="Post">
                            <div class="mb-3">
                                <label for="Name" class="form-label">Full Name</label>
                                <input type="text" class="form-control" id="Name" asp-for="@Model.account.Name" value="@Model.account.Name" required>
                            </div>
                            <div class="mb-3">
                                <label for="Username" class="form-label">Email</label>
                                <input type="text" class="form-control" id="Username" asp-for="@Model.account.Email" value="@Model.account.Email" required />
                            </div>
                            <div class="mb-3">
                                <label for="Phonenumber" class="form-label">Phone Number</label>
                                <input type="text" class="form-control" id="phonenumber" asp-for="@Model.account.Phone" value="@Model.account.Phone" required>
                            </div>
                            <div class="mb-3">
                                <label for="Username" class="form-label">Username</label>
                                <input type="text" class="form-control" id="Username" asp-for="@Model.account.Username" value="@Model.account.Username" required />
                            </div>
                            <div class="mb-3">
                                <label for="Phonenumber" class="form-label">Password</label>
                                <input type="password" class="form-control" asp-for="@Model.account.Password" value="@Model.account.Password" required>
                            </div>
                            <div class="mb-3">
                                <label for="Address" class="form-label">Address</label>
                                <input type="text" class="form-control" id="Address" name="address" value="@Model.account.Address" required>
                            </div>
                            <div class="mb-3">
                                <label for="Address" class="form-label">Role</label>
                                <select class="form-control" asp-for="@Model.account.RoleId">
                                    @foreach (var role in Model.listRole)
                                    {
                                        <option asp-for="@Model.account.RoleId" value="@role.RoleId"> @role.RoleName </option>
                                    }
                                </select>
                            </div>
                            <div class="text-center mt-3">
                                <button type="submit" class="btn btn-primary py-2 px-4">Add New User</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    @{
        var errorMessage = TempData["ErrorMessage"] as string;
    }
    @if (!string.IsNullOrEmpty(errorMessage))
    {
        <div>
            <div id="toastNotification" class="toast align-items-center text-bg-danger border-0 show" role="alert" aria-live="assertive" aria-atomic="true" style="position: fixed; top: 20px; right: 20px; z-index: 1050; border-radius: 10px; overflow: hidden; width: 350px; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);">
                <div class="d-flex">
                    <div class="toast-body p-3" style="font-size: 16px; line-height: 1.4; color: #666565;">
                        @errorMessage
                    </div>
                </div>
                <!-- Progress bar for countdown -->
                <div class="toast-progress-bar" style="height: 5px; background-color: rgba(255, 255, 255, 0.3);"></div>
            </div>
        </div>
    }
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var toastElement = document.getElementById("toastNotification");
            var progressBar = document.createElement('div');
            progressBar.classList.add("toast-progress-bar");
            progressBar.style = "position: absolute; bottom: 0; left: 0; height: 5px; background-color: orange;";
            toastElement.appendChild(progressBar);

            var duration = 4000;
            var startTime = Date.now();

            if (toastElement) {
                var interval = setInterval(function () {
                    var elapsedTime = Date.now() - startTime;
                    var progress = (elapsedTime / duration) * 100;

                    progressBar.style.width = progress + "%";

                    if (elapsedTime >= duration) {
                        clearInterval(interval);
                        var toast = new bootstrap.Toast(toastElement);
                        toast.hide();
                    }
                }, 100);
            }
        });
    </script>
    <!-- Template Javascript -->
    <script src="~/js/main.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $("#Address").on("input", function () {
                var query = $(this).val();
                console.log("User input:", query);

                if (query.length > 2) {
                    $.ajax({
                        url: "http://localhost:7031/api/Location/Search",
                        type: "GET",
                        data: { address: query },
                        success: function (data) {
                            console.log("API Response (Raw):", data);

                            $("#suggestions").html("");
                            let addresses = [];

                            if (data && data.$values && Array.isArray(data.$values)) {
                                addresses = data.$values;
                            } else {
                                console.log("API response is not in expected format:", data);
                                return;
                            }

                            addresses.forEach(function (item) {
                                $("#suggestions").append(`<option value="${item}">`);
                            });
                        },
                        error: function (xhr, status, error) {
                            console.log("AJAX Error:", xhr.status, xhr.statusText);
                            console.log("Response Text:", xhr.responseText);
                        }
                    });
                }
            });
        });
    </script>
</body>

</html>

